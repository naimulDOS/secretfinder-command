1.not a file save command only -v
cat sohow.txt | while read url; do python3 SecretFinder/SecretFinder.py -i "$url" -o cli ; done

2command save file 
cat sohow.txt | while read url; do python3 SecretFinder/SecretFinder.py -i "$url" -o cli ;>result.txt done
